{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd web-dashboard && npm install && npm run build",
  "outputDirectory": "web-dashboard/dist/web-dashboard/browser",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
